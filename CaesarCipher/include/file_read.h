#include <string>
#include <vector>
#include <set>
using namespace std;

string read_from_file(string);

void write_to_file(string, string);

vector<string> read_from_file_to_vec(string);
set<string> read_from_file_to_set(string);